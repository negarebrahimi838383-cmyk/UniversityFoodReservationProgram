# Use Case Diagram – سامانه رزرو غذا

در این سند، دیاگرام موارد استفاده ارائه می‌شود.

```mermaid
%%{init: {'theme':'default'}}%%
usecase
  actor Student as S
  actor Admin as A
  actor Kitchen as K

  S --> (ثبت‌نام / ورود)
  S --> (مشاهده منو)
  S --> (رزرو غذا)
  S --> (لغو رزرو)
  S --> (مشاهده تاریخچه سفارش)

  A --> (مدیریت منو)
  A --> (مشاهده گزارش‌ها)

  K --> (مشاهده سفارش‌ها)
  K --> (به‌روزرسانی وضعیت سفارش)
